<UserControl x:Class="Janus.App.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Results:" FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding EventCount, StringFormat='({0} events found)'}" FontWeight="Bold" Margin="0,0,10,0"/>
            <CheckBox Content="Error" IsChecked="{Binding FilterError}" Margin="0,0,10,0"/>
            <CheckBox Content="Warning" IsChecked="{Binding FilterWarning}" Margin="0,0,10,0"/>
            <CheckBox Content="Info" IsChecked="{Binding FilterInfo}" Margin="0,0,10,0"/>
            <TextBox Width="200" Text="{Binding SearchText}" Margin="10,0,0,0"/>
            <Button Content="Save Snapshot" Command="{Binding SaveSnapshotCommand}" Width="120" Margin="20,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
            <TextBlock Text="Machine: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Metadata.MachineName}" Margin="5,0,10,0"/>
            <TextBlock Text="Notes: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Metadata.UserNotes}" Margin="5,0,10,0"/>
            <TextBlock Text="Scan Timestamp: " FontWeight="Bold"/>
            <TextBlock Text="{Binding MetadataTimestampLocal}" Margin="5,0,10,0"/>
            <TextBlock Text="Window: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Metadata.Window}" Margin="5,0,10,0"/>
            <TextBlock Text="Snapshot Created: " FontWeight="Bold"/>
            <TextBlock Text="{Binding MetadataSnapshotCreatedLocal}" Margin="5,0,10,0"/>
            <TextBlock Text="Minutes Before: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Metadata.MinutesBefore}" Margin="5,0,10,0"/>
            <TextBlock Text="Minutes After: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Metadata.MinutesAfter}" Margin="5,0,10,0"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredEvents}" SelectedItem="{Binding SelectedEvent}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding TimeCreatedLocal}" Width="150"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="120"/>
                <DataGridTextColumn Header="Event ID" Binding="{Binding EventId}" Width="80"/>
                <DataGridTextColumn Header="Log Name" Binding="{Binding LogName}" Width="120"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Level}" Value="Error">
                            <Setter Property="Background" Value="LightCoral"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                            <Setter Property="Background" Value="Khaki"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="Info">
                            <Setter Property="Background" Value="LightGreen"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Copy Message" Command="{Binding CopyMessageCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
        <GroupBox Grid.Row="2" Header="Event Details" Margin="0,0,0,0">
            <StackPanel>
                <TextBlock Text="Timestamp:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedEvent.TimeCreatedLocal}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding SelectedEvent.Message}" IsReadOnly="True" TextWrapping="Wrap" Height="100"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
