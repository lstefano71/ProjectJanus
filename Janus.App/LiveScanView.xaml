<UserControl x:Class="Janus.App.LiveScanView"
    xmlns:local="clr-namespace:Janus.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="800">
    <UserControl.Resources>
        <local:StringNotEmptyToVisibleConverter x:Key="StringNotEmptyToVisible" />
        <local:FirstLineConverter x:Key="FirstLineConverter" />
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Timestamp of Interest:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding Timestamp}" Width="140"/>
            <TextBox Text="{Binding TimeOfDay}" Width="100" Margin="10,0,0,0"/>
            <Button Content="Now" Command="{Binding SetNowCommand}" Margin="10,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Minutes Before:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding MinutesBefore}" Width="60"/>
            <TextBlock Text="Minutes After:" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <TextBox Text="{Binding MinutesAfter}" Width="60"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
            <Button Content="Scan Events" Command="{Binding ScanCommand}" Width="120"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" Margin="10,0,0,0"/>
        </StackPanel>
        <TextBlock Grid.Row="3" Text="{Binding ScanStatus}" FontWeight="Bold" Foreground="DarkBlue"/>
        <Grid Grid.Row="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding Events}"
                      SelectedItem="{Binding SelectedEvent, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      Margin="0,0,0,0"
                      RowHeight="28">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeCreated}" Width="150"/>
                    <DataGridTextColumn Header="Log" Binding="{Binding LogName}" Width="120"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="120"/>
                    <DataGridTextColumn Header="EventId" Binding="{Binding EventId}" Width="80"/>
                    <DataGridTextColumn Header="Message" Width="*">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Message">
                                <Binding.Converter>
                                    <local:FirstLineConverter />
                                </Binding.Converter>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#DDD" ShowsPreview="True" />
            <Grid Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" MinWidth="120" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="2*" MinWidth="200" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Stretch" Margin="0,0,0,0">
                    <TextBlock FontWeight="SemiBold"><Run Text="Time: "/><Run Text="{Binding SelectedEvent.TimeCreated}"/></TextBlock>
                    <TextBlock><Run Text="Log: "/><Run Text="{Binding SelectedEvent.LogName}"/></TextBlock>
                    <TextBlock><Run Text="Level: "/><Run Text="{Binding SelectedEvent.Level}"/></TextBlock>
                    <TextBlock><Run Text="Source: "/><Run Text="{Binding SelectedEvent.Source}"/></TextBlock>
                    <TextBlock><Run Text="EventId: "/><Run Text="{Binding SelectedEvent.EventId}"/></TextBlock>
                    <TextBlock><Run Text="Machine: "/><Run Text="{Binding SelectedEvent.MachineName}"/></TextBlock>
                </StackPanel>
                <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#DDD" ShowsPreview="True" />
                <TextBox Grid.Column="2"
                         Text="{Binding SelectedEvent.Message}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         FontFamily="Consolas"
                         FontSize="12"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Margin="0,0,0,0"
                         MinHeight="0"
                         MinWidth="0"
                         MaxWidth="Infinity"
                         />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
